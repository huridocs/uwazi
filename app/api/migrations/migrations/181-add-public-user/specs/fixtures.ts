import { Fixture } from '../types';

export const fixtures: Fixture = {
  settings: [
    {
      languages: [{ key: 'en', label: 'English', default: true }],
      site_name: 'Uwazi test instance',
    },
  ],
};
