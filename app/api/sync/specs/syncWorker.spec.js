import syncWorker from './syncWorker';

describe('syncWorker', () => {
  it('should process the log records newer than the current sync time (minus 1 sec)', () => {
    syncWorker.syncronize();

    expect()
  });
});
