name: Build Test (Simple)

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Test step
        run: echo "Test workflow is working"
